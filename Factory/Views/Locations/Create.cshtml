@model Factory.Models.Location

<h4>Add a new Sillystringz factory location</h4>

@using (Html.BeginForm())
{
  <h4>Location Name</h4>
  @Html.TextBoxFor(model => model.LocationName, new {@required="required"})

  <input type="submit" value="Add new factory Location" class="btn btn-default"/>
}